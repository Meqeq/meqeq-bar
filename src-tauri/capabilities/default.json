{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Capability for the main window",
  "windows": ["main", "bar0", "bar1", "popup0", "popup1"],
  "permissions": [
    "core:default",
    "opener:default",
    "core:event:allow-listen",
    "core:webview:allow-create-webview"
  ]
}
